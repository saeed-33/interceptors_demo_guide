# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and info-level suggestions.
#
# Note: `package:flutter_lints` is intentionally not included because it is not
# a dependency of this demo project. The analyzer still reports language issues
# and deprecation notices (e.g. `withOpacity`).

linter:
  rules:
    # Demo code intentionally uses print() for terminal visibility.
    # avoid_print: false

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
